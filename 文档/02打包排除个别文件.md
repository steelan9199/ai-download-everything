Read 

## 结论

这是“白名单打包”，不是黑名单打包。

证据就在 `package.json:17-24`：

- `build.files` 里只有这几项：
  - `electron/**`
  - `renderer/**`
  - `site-rules/**`
  - `package.json`

这就表示：打包时只会把这些匹配到的文件纳入产物，其他文件默认不会被打进去。它本质上是“允许列表”。

---

## 如果我不想某些文件打进去，怎么做

### 1) 这是白名单，最直接的做法
直接不要把它写进 `build.files` 即可。

比如你不想打包 `temp/**`，只要不加进去就行。

### 2) 如果你已经放进了一个目录，但想排除个别文件
electron-builder 支持“黑名单排除规则”，写法是前面加 `!`：

```json
"build": {
  "files": [
    "electron/**",
    "renderer/**",
    "site-rules/**",
    "package.json",
    "!temp/**",
    "!**/*.log",
    "!**/secret.*"
  ]
}
```

其中：

- `!temp/**` = 排除 temp 目录
- `!**/*.log` = 排除所有日志文件
- `!**/secret.*` = 排除敏感文件

---

## 一句话总结

- 当前项目：白名单打包
- 如果你要排除某些文件，直接在 `package.json:17-24` 的 `build.files` 里加 `!` 排除规则即可
